/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains:["res.cloudinary.com", 'localhost', 'xxxxx.wpengine.com'],
  },
  swcMinify: true,
}

module.exports = nextConfig
